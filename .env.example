# Pick one (or both). RAWDOG_PROVIDER picks which to use: "openai" (default) or "anthropic"
RAWDOG_PROVIDER=openai

OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-codex

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-7
